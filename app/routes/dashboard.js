module.exports = function(app) {
  /**const admin = app.api.admin;
  const estabelecimento = app.api.estabelecimento;
  const login  = app.api.login;

  app.route('/login')
     .get(login.getLogin)
     .post(login.autenticaLogin);

//  app.use('/dashboard', login.verificarToken);//AUTENTICA

  app.route('/dashboard')
     .get(admin.getDashboard);

  app.route('/dashboard/estabelecimento')
     .get(estabelecimento.getAllEstabelecimentos)
     .post(estabelecimento.createEstabelecimento);

  app.route('/dashboard/estabelecimento/:id')
     .get(estabelecimento.getAllEstabelecimentos)
     .put(estabelecimento.atualizaEstabelecimento);

  app.route('/dashboard/admin')
     .get(admin.getAllAdmins)
     .post(admin.createAdmin);

  app.route('/dashboard/admin/:id')
     .get(admin.getAdminById)
     .put(admin.atualizaAdmin);


  app.delete('/dashboard/logout',  admin.logout);
  */

}
